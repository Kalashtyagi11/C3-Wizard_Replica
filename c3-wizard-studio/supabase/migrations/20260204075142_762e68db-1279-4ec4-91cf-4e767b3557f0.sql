-- Fix: allow authenticated users to use the identity sequence for inserts
-- Required for tables with GENERATED BY DEFAULT AS IDENTITY / SERIAL-like behavior

GRANT USAGE, SELECT, UPDATE ON SEQUENCE public.c3_self_employed_id_seq TO authenticated;